{
  "name": "delhiglow-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace=frontend && rm -rf build && cp -r frontend/build build",
    "install-all": "npm install"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
